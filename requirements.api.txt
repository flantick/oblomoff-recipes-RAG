# Dependencies of the API container (Step 6). torch is installed separately in
# the Dockerfile (CPU wheel). Only what retrieval + generation + web need is here.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
python-dotenv>=1.0.1
loguru>=0.7.2
qdrant-client>=1.12
FlagEmbedding>=1.3
transformers>=4.44
openai>=1.40
