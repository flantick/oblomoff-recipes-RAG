[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers
xfail_strict = true
markers =
    slow: тест заметно медленнее остальных
filterwarnings =
    ignore::DeprecationWarning:qdrant_client.*
